rem Command to compile the code for Windows ✨
rem ⚠ You'll need to install `clang` and have it in PATH ⚠

rem Run the generated executable using `main` 📝

clang++ ../src/main.cc -o ../main.exe
